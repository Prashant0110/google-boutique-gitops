apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
- name: application-overview-dashboard
  namespace: monitoring
  files:
  - application-overview.json=../json/application-overview.json
  generatorOptions:
    labels:
      grafana_dashboard: "1"
